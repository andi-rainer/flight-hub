/**
 * Layout for account-inactive page
 * Simple centered layout without sidebar/header
 */
export default function AccountInactiveLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
